<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Cuidar el Alma · Un momento de calma, cada día</title>
<style>
  @import url('https://fonts.googleapis.com/css2?family=Lora:ital,wght@0,400;0,500;0,600;1,400&family=Inter:wght@400;500;600;700&display=swap');

  :root {
    --primary: #5B8FB9;
    --primary-dark: #3D6E95;
    --primary-light: #B8D4E3;
    --secondary: #94B89C;
    --secondary-dark: #6C9A76;
    --accent: #E8B86A;
    --accent-dark: #C99A4A;
    --bg: #FBF6EC;
    --bg-alt: #F5EEDD;
    --card: #FFFFFF;
    --text-strong: #3D2E26;
    --text-soft: #6B5D55;
    --text-muted: #8B7A65;
    --border: #ECDDC0;
    --shadow: 0 2px 16px rgba(61, 46, 38, 0.06);
    --shadow-lift: 0 8px 28px rgba(61, 46, 38, 0.10);
  }

  * { box-sizing: border-box; margin: 0; padding: 0; }

  html { scroll-behavior: smooth; }

  body {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
    background: var(--bg);
    color: var(--text-strong);
    line-height: 1.65;
    -webkit-font-smoothing: antialiased;
  }

  img, svg { display: block; max-width: 100%; }

  a { color: inherit; text-decoration: none; }

  .container {
    max-width: 1180px;
    margin: 0 auto;
    padding: 0 32px;
  }

  h1, h2, h3 {
    font-family: 'Lora', Georgia, serif;
    font-weight: 500;
    color: var(--text-strong);
    line-height: 1.2;
  }

  /* ============ ARCO OJIVAL — elemento de firma ============ */
  .arch {
    border-radius: 50% 50% 4px 4px / 38% 38% 0 0;
  }
  .arch-sm {
    border-radius: 50% 50% 3px 3px / 42% 42% 0 0;
  }

  /* ============ BOTONES ============ */
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    font-family: 'Inter', sans-serif;
    font-size: 15px;
    font-weight: 600;
    padding: 13px 28px;
    border-radius: 100px;
    border: none;
    cursor: pointer;
    transition: transform 0.15s ease, box-shadow 0.15s ease, background 0.15s ease;
    white-space: nowrap;
  }
  .btn:active { transform: scale(0.97); }
  .btn-primary {
    background: var(--primary);
    color: #FFFFFF;
    box-shadow: 0 4px 14px rgba(91, 143, 185, 0.35);
  }
  .btn-primary:hover { background: var(--primary-dark); box-shadow: 0 6px 18px rgba(91, 143, 185, 0.45); }
  .btn-outline {
    background: transparent;
    color: var(--primary);
    border: 1.5px solid var(--primary);
  }
  .btn-outline:hover { background: rgba(91, 143, 185, 0.08); }
  .btn-secondary {
    background: var(--secondary);
    color: #1F4028;
  }
  .btn-secondary:hover { background: var(--secondary-dark); color: #FFFFFF; }
  .btn-sm { padding: 9px 20px; font-size: 13.5px; }

  /* ============ NAV ============ */
  header {
    position: sticky;
    top: 0;
    z-index: 50;
    background: rgba(251, 246, 236, 0.92);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid var(--border);
  }
  nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 18px 0;
  }
  .logo {
    display: flex;
    align-items: center;
    gap: 10px;
    font-family: 'Lora', serif;
    font-size: 20px;
    font-weight: 600;
    color: var(--text-strong);
  }
  .logo-mark {
    width: 30px;
    height: 34px;
    background: linear-gradient(180deg, var(--primary) 0%, var(--primary-dark) 100%);
    border-radius: 50% 50% 3px 3px / 60% 60% 0 0;
    position: relative;
    flex-shrink: 0;
  }
  .logo-mark::after {
    content: '';
    position: absolute;
    inset: 5px 5px 4px 5px;
    background: var(--accent);
    border-radius: 50% 50% 2px 2px / 60% 60% 0 0;
    opacity: 0.85;
  }
  .nav-links {
    display: flex;
    align-items: center;
    gap: 36px;
    list-style: none;
  }
  .nav-links a {
    font-size: 14.5px;
    font-weight: 500;
    color: var(--text-soft);
    transition: color 0.15s ease;
  }
  .nav-links a:hover { color: var(--primary); }
  .nav-actions { display: flex; align-items: center; gap: 16px; }
  .nav-toggle {
    display: none;
    width: 38px; height: 38px;
    border: none;
    background: transparent;
    cursor: pointer;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: 5px;
  }
  .nav-toggle span {
    width: 22px; height: 2px;
    background: var(--text-strong);
    border-radius: 2px;
  }

  /* ============ HERO ============ */
  .hero {
    padding: 88px 0 100px;
    position: relative;
    overflow: hidden;
  }
  .hero-grid {
    display: grid;
    grid-template-columns: 1.05fr 0.95fr;
    gap: 64px;
    align-items: center;
  }
  .eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-size: 13px;
    font-weight: 600;
    color: var(--accent-dark);
    text-transform: uppercase;
    letter-spacing: 1.2px;
    margin-bottom: 20px;
  }
  .eyebrow::before {
    content: '';
    width: 18px; height: 1.5px;
    background: var(--accent);
  }
  .hero h1 {
    font-size: 50px;
    letter-spacing: -0.5px;
    margin-bottom: 22px;
  }
  .hero h1 em {
    font-style: italic;
    color: var(--primary);
  }
  .hero-sub {
    font-size: 18px;
    color: var(--text-soft);
    max-width: 460px;
    margin-bottom: 36px;
  }
  .hero-actions {
    display: flex;
    gap: 14px;
    flex-wrap: wrap;
    margin-bottom: 40px;
  }
  .hero-trust {
    display: flex;
    align-items: center;
    gap: 14px;
    font-size: 13.5px;
    color: var(--text-muted);
  }
  .avatar-stack { display: flex; }
  .avatar-stack span {
    width: 30px; height: 30px;
    border-radius: 50%;
    border: 2.5px solid var(--bg);
    margin-left: -8px;
    display: flex; align-items: center; justify-content: center;
    font-size: 11px; font-weight: 600; color: #fff;
  }
  .avatar-stack span:first-child { margin-left: 0; }

  /* Hero visual: ventana ojival luminosa */
  .hero-visual {
    position: relative;
    display: flex;
    justify-content: center;
  }
  .window {
    width: 320px;
    height: 420px;
    border-radius: 50% 50% 6px 6px / 38% 38% 0 0;
    background: radial-gradient(circle at 50% 30%, #FDF1D6 0%, var(--primary-light) 45%, var(--primary) 100%);
    position: relative;
    box-shadow: var(--shadow-lift);
    overflow: hidden;
  }
  .window::before {
    content: '';
    position: absolute;
    inset: 0;
    background:
      linear-gradient(115deg, transparent 40%, rgba(232, 184, 106, 0.35) 41%, rgba(232, 184, 106, 0.35) 43%, transparent 44%),
      linear-gradient(65deg, transparent 60%, rgba(255,255,255,0.25) 61%, rgba(255,255,255,0.25) 63%, transparent 64%);
  }
  .window-mullion-v {
    position: absolute;
    top: 0; bottom: 0; left: 50%;
    width: 3px;
    background: rgba(255,255,255,0.55);
    transform: translateX(-50%);
  }
  .window-mullion-h {
    position: absolute;
    left: 0; right: 0; top: 58%;
    height: 3px;
    background: rgba(255,255,255,0.45);
  }
  .window-glow {
    position: absolute;
    width: 130px; height: 130px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(253,241,214,0.95) 0%, rgba(253,241,214,0) 70%);
    top: 18%;
    left: 50%;
    transform: translateX(-50%);
  }
  .window-frame {
    position: absolute;
    inset: 14px;
    border-radius: 50% 50% 4px 4px / 36% 36% 0 0;
    border: 2px solid rgba(255,255,255,0.5);
    pointer-events: none;
  }
  .floating-card {
    position: absolute;
    background: var(--card);
    border-radius: 14px;
    box-shadow: var(--shadow-lift);
    padding: 14px 18px;
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 13px;
    font-weight: 600;
  }
  .floating-card.fc-1 { bottom: -18px; left: -28px; color: var(--text-strong); }
  .floating-card.fc-2 { top: 30px; right: -30px; color: var(--text-strong); }
  .fc-dot {
    width: 10px; height: 10px;
    border-radius: 50%;
    background: var(--secondary);
    flex-shrink: 0;
  }
  .fc-dot.amber { background: var(--accent); }

  /* ============ SECCIÓN: EVANGELIO DEL DÍA ============ */
  .gospel {
    padding: 0 0 100px;
  }
  .gospel-card {
    background: var(--card);
    border-radius: 28px;
    border: 1px solid var(--border);
    box-shadow: var(--shadow);
    overflow: hidden;
    display: grid;
    grid-template-columns: 0.85fr 1.15fr;
  }
  .gospel-visual {
    background: linear-gradient(160deg, var(--primary-light) 0%, var(--accent) 100%);
    position: relative;
    min-height: 320px;
    display: flex;
    align-items: flex-end;
    padding: 28px;
  }
  .gospel-visual-badge {
    position: absolute;
    top: 24px; left: 24px;
    background: rgba(255,255,255,0.9);
    color: var(--text-strong);
    font-size: 12.5px;
    font-weight: 700;
    padding: 7px 16px;
    border-radius: 100px;
    letter-spacing: 0.3px;
  }
  .gospel-visual-date {
    color: #fff;
    font-family: 'Lora', serif;
    font-size: 22px;
    font-weight: 500;
    text-shadow: 0 2px 12px rgba(61,46,38,0.25);
  }
  .gospel-body {
    padding: 48px 52px;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }
  .gospel-ref {
    font-size: 13.5px;
    font-weight: 600;
    color: var(--primary);
    margin-bottom: 10px;
  }
  .gospel-body h2 { font-size: 30px; margin-bottom: 18px; }
  .gospel-quote {
    font-family: 'Lora', serif;
    font-style: italic;
    font-size: 17px;
    color: var(--text-soft);
    line-height: 1.7;
    border-left: 3px solid var(--accent);
    padding-left: 20px;
    margin-bottom: 28px;
  }
  .gospel-actions { display: flex; gap: 12px; flex-wrap: wrap; }

  /* ============ SECCIÓN: EXPLORAR ============ */
  .explore { padding: 0 0 100px; }
  .section-head {
    text-align: center;
    max-width: 560px;
    margin: 0 auto 56px;
  }
  .section-head h2 { font-size: 34px; margin-bottom: 14px; }
  .section-head p { color: var(--text-soft); font-size: 16.5px; }

  .explore-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 24px;
  }
  .explore-card {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: 20px;
    padding: 32px 26px;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }
  .explore-card:hover {
    transform: translateY(-6px);
    box-shadow: var(--shadow-lift);
  }
  .explore-icon {
    width: 54px; height: 58px;
    border-radius: 50% 50% 4px 4px / 42% 42% 0 0;
    display: flex; align-items: center; justify-content: center;
    margin-bottom: 20px;
    font-size: 22px;
  }
  .explore-card h3 { font-size: 18px; margin-bottom: 8px; }
  .explore-card p { font-size: 14px; color: var(--text-soft); margin-bottom: 16px; }
  .explore-link {
    font-size: 13.5px;
    font-weight: 600;
    color: var(--primary);
    display: inline-flex;
    align-items: center;
    gap: 5px;
  }
  .explore-link svg { width: 14px; height: 14px; transition: transform 0.15s ease; }
  .explore-card:hover .explore-link svg { transform: translateX(3px); }

  /* ============ CITA COMUNITARIA ============ */
  .quote-section {
    padding: 90px 0;
    background: var(--bg-alt);
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
  }
  .quote-wrap {
    max-width: 740px;
    margin: 0 auto;
    text-align: center;
  }
  .quote-mark {
    font-family: 'Lora', serif;
    font-size: 64px;
    color: var(--accent);
    line-height: 1;
    margin-bottom: 8px;
  }
  .quote-text {
    font-family: 'Lora', serif;
    font-style: italic;
    font-size: 26px;
    color: var(--text-strong);
    line-height: 1.5;
    margin-bottom: 26px;
  }
  .quote-author {
    font-size: 14.5px;
    color: var(--text-muted);
    font-weight: 500;
  }

  /* ============ CTA FINAL ============ */
  .cta-final {
    padding: 100px 0;
    text-align: center;
  }
  .cta-final h2 { font-size: 36px; margin-bottom: 16px; }
  .cta-final p { color: var(--text-soft); font-size: 17px; margin-bottom: 36px; max-width: 480px; margin-left: auto; margin-right: auto; }
  .cta-final-actions { display: flex; gap: 14px; justify-content: center; flex-wrap: wrap; }

  /* ============ FOOTER ============ */
  footer {
    background: var(--text-strong);
    color: #DCC9B8;
    padding: 64px 0 28px;
  }
  .footer-grid {
    display: grid;
    grid-template-columns: 1.4fr 1fr 1fr 1fr;
    gap: 40px;
    padding-bottom: 44px;
    border-bottom: 1px solid rgba(220, 201, 184, 0.18);
  }
  .footer-logo {
    display: flex; align-items: center; gap: 10px;
    font-family: 'Lora', serif; font-size: 19px; font-weight: 600;
    color: #FBF6EC;
    margin-bottom: 14px;
  }
  .footer-about { font-size: 13.5px; color: #B8A893; max-width: 240px; line-height: 1.7; }
  .footer-col h4 {
    font-family: 'Inter', sans-serif;
    font-size: 12.5px;
    text-transform: uppercase;
    letter-spacing: 0.8px;
    color: #B8A893;
    margin-bottom: 16px;
    font-weight: 600;
  }
  .footer-col ul { list-style: none; display: flex; flex-direction: column; gap: 11px; }
  .footer-col a { font-size: 14px; color: #DCC9B8; transition: color 0.15s ease; }
  .footer-col a:hover { color: var(--accent); }
  .footer-bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-top: 24px;
    font-size: 12.5px;
    color: #8B7A65;
    flex-wrap: wrap;
    gap: 12px;
  }

  /* ============ ANIMACIÓN DE ENTRADA (en carga, no en scroll) ============ */
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(14px); }
    to { opacity: 1; transform: translateY(0); }
  }
  .fade-in-1 { animation: fadeUp 0.7s ease both; }
  .fade-in-2 { animation: fadeUp 0.7s ease 0.12s both; }
  .fade-in-3 { animation: fadeUp 0.7s ease 0.22s both; }

  @media (prefers-reduced-motion: reduce) {
    html { scroll-behavior: auto; }
    .fade-in-1, .fade-in-2, .fade-in-3 { animation: none; }
  }

  /* ============ RESPONSIVE ============ */
  @media (max-width: 980px) {
    .hero-grid { grid-template-columns: 1fr; }
    .hero-visual { order: -1; margin-bottom: 36px; padding: 0 16px; }
    .window { width: 240px; height: 320px; }
    .gospel-card { grid-template-columns: 1fr; }
    .gospel-visual { min-height: 200px; }
    .explore-grid { grid-template-columns: repeat(2, 1fr); }
    .footer-grid { grid-template-columns: 1fr 1fr; }
  }

  @media (max-width: 720px) {
    .nav-links { display: none; }
    .nav-toggle { display: flex; }
    .container { padding: 0 22px; }
    .logo { font-size: 17px; gap: 8px; }
    .logo-mark { width: 24px; height: 27px; }
    .nav-actions { gap: 8px; }
    .hero { padding: 40px 0 56px; }
    .hero h1 { font-size: 32px; }
    .hero-sub { font-size: 16px; }
    .floating-card {
      font-size: 11.5px;
      padding: 9px 13px;
      gap: 7px;
    }
    .floating-card.fc-1 { bottom: -14px; left: 0; }
    .floating-card.fc-2 { top: 20px; right: 0; }
    .gospel-body { padding: 32px 26px; }
    .gospel-body h2 { font-size: 24px; }
    .explore-grid { grid-template-columns: 1fr; }
    .quote-text { font-size: 21px; }
    .footer-grid { grid-template-columns: 1fr; gap: 28px; }
    .cta-final h2 { font-size: 28px; }
    .section-head h2 { font-size: 27px; }
  }

  @media (max-width: 400px) {
    .window { width: 200px; height: 270px; }
    .floating-card { font-size: 10.5px; padding: 8px 11px; }
  }
</style>
</head>
<body>

<header>
  <div class="container">
    <nav>
      <a href="#" class="logo">
        <span class="logo-mark" aria-hidden="true"></span>
        Cuidar el Alma
      </a>
      <ul class="nav-links">
        <li><a href="#evangelio">Evangelio</a></li>
        <li><a href="#explorar">Explorar</a></li>
        <li><a href="#comunidad">Comunidad</a></li>
        <li><a href="#">Premium</a></li>
      </ul>
      <div class="nav-actions">
        <a href="#" class="btn btn-primary btn-sm">Iniciar sesión</a>
        <button class="nav-toggle" aria-label="Abrir menú">
          <span></span><span></span><span></span>
        </button>
      </div>
    </nav>
  </div>
</header>

<main>

  <!-- ===================== HERO ===================== -->
  <section class="hero">
    <div class="container hero-grid">
      <div class="fade-in-1">
        <p class="eyebrow">Compañía espiritual diaria</p>
        <h1>Un momento de calma, <em>cada día</em></h1>
        <p class="hero-sub">
          Lecturas, oraciones y reflexiones para acompañar tu fe y la de tu familia,
          desde los más pequeños hasta los más grandes de casa.
        </p>
        <div class="hero-actions">
          <a href="#evangelio" class="btn btn-primary">Leer el Evangelio de hoy</a>
          <a href="#explorar" class="btn btn-outline">Conocer la plataforma</a>
        </div>
        <div class="hero-trust">
          <div class="avatar-stack" aria-hidden="true">
            <span style="background:#5B8FB9;">MR</span>
            <span style="background:#94B89C;">AT</span>
            <span style="background:#E8B86A;">JP</span>
          </div>
          <span>Ya acompaña a más de 12.000 familias</span>
        </div>
      </div>

      <div class="hero-visual fade-in-2">
        <div class="window">
          <div class="window-glow"></div>
          <div class="window-mullion-v"></div>
          <div class="window-mullion-h"></div>
          <div class="window-frame"></div>
        </div>
        <div class="floating-card fc-1">
          <span class="fc-dot"></span> Oración guardada
        </div>
        <div class="floating-card fc-2">
          <span class="fc-dot amber"></span> Nuevo · Domingo
        </div>
      </div>
    </div>
  </section>

  <!-- ===================== EVANGELIO DEL DÍA ===================== -->
  <section class="gospel" id="evangelio">
    <div class="container">
      <div class="gospel-card">
        <div class="gospel-visual">
          <span class="gospel-visual-badge">Domingo</span>
          <span class="gospel-visual-date">28 de junio</span>
        </div>
        <div class="gospel-body">
          <p class="gospel-ref">Evangelio según San Juan · 10, 11–18</p>
          <h2>El Buen Pastor</h2>
          <p class="gospel-quote">
            "Yo soy el buen pastor. El buen pastor da su vida por las ovejas.
            Conozco a las mías y las mías me conocen a mí."
          </p>
          <div class="gospel-actions">
            <a href="#" class="btn btn-primary btn-sm">Leer completo</a>
            <a href="#" class="btn btn-outline btn-sm">Escuchar audio</a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===================== EXPLORAR ===================== -->
  <section class="explore" id="explorar">
    <div class="container">
      <div class="section-head">
        <h2>Todo lo que necesitas para crecer en tu fe</h2>
        <p>Cuatro espacios pensados para acompañarte en distintos momentos del día y de la vida.</p>
      </div>
      <div class="explore-grid">

        <div class="explore-card">
          <div class="explore-icon" style="background:#E6F1F9;">☀️</div>
          <h3>Domingos</h3>
          <p>El evangelio de cada domingo, explicado con sencillez para toda la familia.</p>
          <a href="#" class="explore-link">Explorar
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
          </a>
        </div>

        <div class="explore-card">
          <div class="explore-icon" style="background:#E8F2EA;">📖</div>
          <h3>Biblioteca</h3>
          <p>Relatos, oraciones y catequesis organizadas por edad e intereses.</p>
          <a href="#" class="explore-link">Explorar
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
          </a>
        </div>

        <div class="explore-card">
          <div class="explore-icon" style="background:#FBF0DC;">🕊️</div>
          <h3>Oraciones</h3>
          <p>Un momento de quietud guiado, disponible cuando lo necesites.</p>
          <a href="#" class="explore-link">Explorar
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
          </a>
        </div>

        <div class="explore-card">
          <div class="explore-icon" style="background:#F7E0DC;">🤝</div>
          <h3>Comunidad</h3>
          <p>Encuentra y conecta con tu parroquia mediante un sencillo código QR.</p>
          <a href="#" class="explore-link">Explorar
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
          </a>
        </div>

      </div>
    </div>
  </section>

  <!-- ===================== CITA COMUNITARIA ===================== -->
  <section class="quote-section" id="comunidad">
    <div class="container">
      <div class="quote-wrap">
        <p class="quote-mark" aria-hidden="true">"</p>
        <p class="quote-text">
          Cada noche, mis hijos piden leer juntos la oración del día.
          Se ha vuelto un pequeño ritual de paz antes de dormir.
        </p>
        <p class="quote-author">— Una madre de la comunidad</p>
      </div>
    </div>
  </section>

  <!-- ===================== CTA FINAL ===================== -->
  <section class="cta-final">
    <div class="container">
      <div>
        <h2>Empieza tu momento de calma hoy</h2>
        <p>Crear una cuenta es gratuito y toma menos de un minuto.</p>
        <div class="cta-final-actions">
          <a href="#" class="btn btn-primary">Crear cuenta gratis</a>
          <a href="#" class="btn btn-secondary">Ver planes Premium</a>
        </div>
      </div>
    </div>
  </section>

</main>

<footer>
  <div class="container">
    <div class="footer-grid">
      <div>
        <a href="#" class="footer-logo">
          <span class="logo-mark" aria-hidden="true"></span>
          Cuidar el Alma
        </a>
        <p class="footer-about">
          Una plataforma de acompañamiento espiritual católico para toda la familia,
          disponible cada día del año.
        </p>
      </div>
      <div class="footer-col">
        <h4>Explorar</h4>
        <ul>
          <li><a href="#evangelio">Domingos</a></li>
          <li><a href="#explorar">Biblioteca</a></li>
          <li><a href="#">Oraciones</a></li>
          <li><a href="#">Premium</a></li>
        </ul>
      </div>
      <div class="footer-col">
        <h4>Comunidad</h4>
        <ul>
          <li><a href="#">Tu parroquia</a></li>
          <li><a href="#">Código QR</a></li>
          <li><a href="#comunidad">Testimonios</a></li>
        </ul>
      </div>
      <div class="footer-col">
        <h4>Ayuda</h4>
        <ul>
          <li><a href="#">Centro de ayuda</a></li>
          <li><a href="#">Contacto</a></li>
          <li><a href="#">Privacidad</a></li>
        </ul>
      </div>
    </div>
    <div class="footer-bottom">
      <span>© 2026 Cuidar el Alma. Todos los derechos reservados.</span>
      <span>Hecho con calma por el equipo DEXUS</span>
    </div>
  </div>
</footer>

<script>
  // Menú móvil simple
  document.querySelector('.nav-toggle').addEventListener('click', function () {
    var links = document.querySelector('.nav-links');
    var visible = links.style.display === 'flex';
    links.style.display = visible ? 'none' : 'flex';
    links.style.cssText += visible
      ? ''
      : 'position:absolute; top:64px; left:0; right:0; flex-direction:column; background:#FBF6EC; padding:24px 32px; border-bottom:1px solid #ECDDC0; gap:18px;';
  });

  // Scroll suave ya gestionado vía CSS (scroll-behavior)
</script>

</body>
</html>